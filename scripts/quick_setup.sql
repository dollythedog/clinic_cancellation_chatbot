-- Quick Database Setup for Clinic Cancellation Chatbot
-- Run this in your existing PostgreSQL instance

-- Create the database (if you have permission)
-- CREATE DATABASE clinic_chatbot;

-- Or if you want to use an existing database, just connect to it and run schema.sql

-- Connect to the database:
-- \c clinic_chatbot

-- Then run:
-- \i C:/Projects/clinic_cancellation_chatbot/scripts/schema.sql

-- Or if you don't want to create a new database, you can use a schema:
-- CREATE SCHEMA IF NOT EXISTS clinic_chatbot;
-- SET search_path TO clinic_chatbot;
-- Then run the schema.sql commands
