# ============================================================================
# Clinic Cancellation Chatbot - Python Dependencies
# ============================================================================
# Install with: pip install -r requirements.txt
# ============================================================================

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.10.0
pydantic-settings>=2.7.0

# Database
sqlalchemy>=2.0.23
psycopg[binary]>=3.2.0
alembic>=1.12.1

# Twilio SMS
twilio==8.10.0

# Scheduling
apscheduler==3.10.4

# Dashboard
streamlit==1.51.0
plotly==5.18.0
pandas>=2.0.0

# Configuration
python-dotenv==1.0.0

# HTTP Client
httpx==0.25.2
requests==2.31.0

# Time Zone Support
pytz==2023.3
python-dateutil==2.8.2

# Validation & Parsing
phonenumbers==8.13.26
email-validator==2.1.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
faker==20.1.0

# Code Quality
# ruff is the canonical linter/formatter per the Design Schematic §4
# (Quality Automation lens). black / flake8 / isort are retained here
# temporarily; cleanup tracked in ISSUES.md and will land with QA-01.
ruff>=0.6.0
black==23.11.0
flake8==6.1.0
mypy==1.7.1
isort==5.12.0

# Logging & Monitoring
python-json-logger==2.0.7
structlog==23.2.0
# pywin32 enables the NTEventLogHandler used by app/infra/logging_config.py
# on Windows production hosts. The marker keeps it off non-Windows installs
# where the Event Log sink gracefully degrades to a no-op. Added for
# Build Slice 2026-04-20-02 (WBS APP-05).
pywin32>=306; sys_platform == 'win32'

# Security
cryptography==41.0.7
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Utilities
python-multipart==0.0.6
jinja2==3.1.2
click==8.1.7

# Development Tools
ipython==8.18.1
ipdb==0.13.13

# ============================================================================
# Optional Dependencies (uncomment if needed)
# ============================================================================

# Redis (for distributed task queue in future)
# redis==5.0.1
# celery==5.3.4

# Monitoring
# sentry-sdk[fastapi]==1.38.0
# prometheus-client==0.19.0

# ML/Analytics (Phase 3)
# scikit-learn==1.3.2
# numpy==1.26.2

# ============================================================================
# Notes
# ============================================================================
# - Versions are pinned for reproducibility
# - Update regularly for security patches
# - Test after updating dependencies
# - Use virtual environment for isolation
# ============================================================================
